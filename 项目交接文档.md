# 靓仔文案工作台 · 项目交接文档

> 交接日期：2026-08-25
> 交接人：WorkBuddy（靓仔）
> 项目状态：已运行数周，核心链路全部打通，正在持续迭代

---

## 一、项目背景与定位

「靓仔文案工作台」是一款面向**玄学命理垂类短视频口播创作者**的一站式内容生产桌面工具。

**解决的核心问题**：老板（账号主理人，深耕玄学命理 8 年的实战派）的日常生产链路是：刷对标账号 → 扒爆款视频文案 → 洗稿改写 → 配音 → 发布 → 盯数据 → 复盘优化。这些环节原本分散在浏览器、记事本、剪辑软件、抖音后台之间，靠人工搬运。本工具把这些环节收进一个仿微信的独立窗口，每个环节都有"AI 专家团"参与，形成闭环。

**产品形态**：单文件桌面 exe（PyInstaller onefile），双击即用，界面仿微信聊天窗（左侧会话列表 + 右侧聊天区），所有 AI 协作都以"群聊对话"形式呈现。

---

## 二、技术栈

| 层 | 技术 | 说明 |
|---|---|---|
| 后端 | Python + Flask + SSE | server.py 为主入口，各功能独立 server 模块 |
| 前端 | 原生 HTML/CSS/JS 单文件 | `web/index.html`（约 8800+ 行），EventSource 接收 SSE 流 |
| 桌面壳 | pywebview | 仿微信独立窗口，WebView2 内核 |
| 打包 | PyInstaller onefile | `copywriters_chat.spec`，产物约 107MB |
| 主 LLM | deeprouter 网关 `gpt-5.4-high` | OpenAI 兼容 API，负责节奏把控/标题等 |
| 专家 LLM | DeepSeek 官方 `deepseek-chat` / `deepseek-reasoner` | 8 位文案专家 + 记录员 + 数据专员 + 原则审查员 |
| 抖音抓取 | f2 库 | 作品列表 + 数据指标抓取 |
| 微博抓取 | weibo.com ajax 接口（getWaterFallContent） | 需登录态 cookie（SUB），手动配置优先于浏览器读取 |
| TTS | IndexTTS-2.5（modelscope） | 本地推理，auto 模式按 GPU 检测自动选路 |
| OCR | RapidOCR | 知识库图片离线识别 |
| 音频转写 | ASR（多源重试） | 音乐源转空自动切真实视频源 |

**LLM 路由规则**（server.py `_resolve_member_provider`）：模型名以 `deepseek-` 开头 → 走 config.deepseek（api.deepseek.com 官方）；其他 → 走 config.api（deeprouter 网关）。也就是说：**洗稿/群聊的专家发言全部走 DeepSeek 官方，主 API（gpt-5.4-high）只用于节奏把控、标题生成等少数环节**。

---

## 三、项目结构与关键路径

```
copywriters-hub/
├── desktop_app.py          # 桌面程序入口（pywebview + 单实例互斥 + 数据目录选择）
├── server.py               # 后端主路由（Flask + SSE + 各功能路由注册）
├── copywriters_chat.spec   # PyInstaller 打包配置
├── config.json             # 主配置（API/专家人设/方法论开关，含密钥，不入库）
├── web/index.html          # 前端单文件（全部界面 + 逻辑）
├── scripts/                # 51 个 py 模块
│   ├── rewrite_flow.py     # 洗稿工坊完整流程（骨架→全员分析→分区补写→审查→评论迭代→最终审查）
│   ├── agents.py           # Agent 类 + LLM 调用（含输入长度兜底、硬超时、重试）
│   ├── extract_server.py   # 扒文案：视频下载→ASR→说话人区分→逐句稿
│   ├── works_library_server.py  # 作品库
│   ├── tts_server.py       # 配音工坊（IndexTTS 本地推理）
│   ├── agent_chat_server.py     # 专家群聊（7 专家 + 记录员）
│   ├── monitor_server.py   # 对标监控（5 分钟轮询）
│   ├── radar_server.py     # 选题雷达
│   ├── api_settings_server.py   # 设置（API 配置、数据目录迁移）
│   ├── rewrite_store.py / works_store.py / skeleton_store.py 等 # 各功能存储
│   ├── skill_methods.py    # 技能方法论注入
│   ├── stats_store.py / data_insight_store.py / learn_store.py  # 复盘/反馈/学习档案
│   └── ...（test_/e2e_/_repro_/_verify_/_patch_ 前缀为测试与修复草稿，不参与运行）
├── monitor/                # 对标监控抓取（fetch.py 抖音+微博抓取核心）
├── knowledge/skills_methods/   # 4 份方法论档案（降AI味/标题公式/合规红线/爆款四基因）
├── knowledge/agent_*/      # 原始知识库（安先生/三把刀/薛辉等实战素材）
├── knowledge_digests/      # 8 位专家内化知识档案 + lessons 学习档案
├── web/、icon.ico、README.md、CODEBUDDY.md、SESSION-STATE.md、软件功能全景梳理.md
└── 数据目录（见第六节，不在源码目录）
```

---

## 四、9 大功能视图

1. **工作台**（首页：消息通知、快捷入口）
2. **洗稿工坊**：AI 全流程洗稿（骨架→全员分析→分区补写→逐句评论迭代→最终审查+分工记录），核心链路见下节
3. **配音工坊**：TTS 本地配音（IndexTTS，auto GPU 检测、多音色预设）
4. **专家群聊**：7 位文案专家 + 记录员阿记，三轮讨论
5. **作品库**：扒文案入库（抖音/微博）+ 说话人 A/B 区分 + 逐句稿
6. **对标监控**：对标账号定时轮询（5 分钟）+ 数据追踪 + 选题雷达
7. **骨架库**：爆款文案骨架沉淀
8. **数据复盘**：真实数据回填 → 数据专员阿数句子级归因 → 专家正确率统计
9. **学习档案**：从爆款中学习提炼（反 AI 幻觉：原文摘录必须逐字校验）
10. **设置**：API 配置、专家人设、技能方法论开关、微博 Cookie、数据目录迁移

---

## 五、核心业务链路

### 洗稿工坊（最核心，7 阶段）
1. **阿骨拆骨架**（deepseek-chat）：拆解原稿结构骨架
2. **全员分析**（9 人并发 3 路）：8 专家 + 阿数，输出爆点/炸点/争议点/共鸣点/情绪点/不可动句子
3. **分区补写**：每个区域由"负责人"专家补写（owner_record 记录分工）
4. **阿审审查**：原则审查员对照原则逐条审查
5. **用户逐区评论迭代**：评论 → 负责人重写（SSE 流式更新句子，支持排队+回车发送）
6. **最终审查 + 阿数记录分工**
7. **数据回填 → 复盘**：满 3 篇作品后阿数建立评价标准 → 负责人替换（winrate 低换人）

**洗稿关键机制**：
- **不可动句（untouchable）**：专家共识锁定的原句，成品必须原样保留，前端 🔒 角标
- **输入长度兜底**：`_MAX_MSG_CHARS=30000` 双层防线（先压 knowledge 档案，再按比例截 user 消息）
- **知识档案截断**：`_KNOWLEDGE_CAP_CHARS=12000`
- **硬超时**：Agent.say() 线程级 join(timeout)，防 TCP 半开挂起卡死流程

### 扒文案链路（作品库）
抖音/微博链接 → f2/微博 ajax 抓视频 → 下载 → ASR 转写（多源重试：音乐源转空自动换真实视频源）→ 说话人 A/B 区分 → 逐句稿入库

### 数据复盘链路
发布后回填真实数据（留存曲线/2秒跳出率/完播率等）→ 阿数句子级归因（黑榜句子+点名专家）→ 记录员阿记评估每条改动有效/无效 → 更新专家正确率 → 反馈进历史档案（负面清单+正面清单）

---

## 六、配置与数据目录

- **主配置**：`config.json`（含 API key，**绝不入库/不随包发布**）
  - `api`：deeprouter 主通道（gpt-5.4-high）
  - `deepseek`：DeepSeek 官方通道（专家用）
  - `agents[]`：8 位专家定义（persona/criteria/model/knowledge_path/digest_path）
  - `skill_methods`：方法论注入开关（max_chars=4200，4 份共约 4000 字）
  - `discussion.max_context_chars`：35000
- **数据目录**（运行时）：`%APPDATA%\靓仔文案工作台\`
  - `config.json`：用户覆盖配置（与源码 config 合并）
  - `output/`：rewrites.json / works.json / skeletons.json / data_insights.json / notifications.json / agent_chat/ / sessions/ 等全部业务数据
  - `desktop.log`：启动/关闭日志
  - **迁移机制**：设置里「迁移到其他盘」→ `%LOCALAPPDATA%\靓仔文案工作台\data_dir.txt` 持久化（优先级：env WB_DATA_DIR > 持久化配置 > 默认 AppData）
- **微博 Cookie**：`output/weibo_cookie.json`（设置界面可配置，手动 cookie 优先于浏览器读取）

---

## 七、产品硬约束（改代码必须遵守）

1. **洗稿工坊与配音工坊是两条独立链路，不能串联**（老板明确拍板过）
2. **反 AI 幻觉**：爆款学习原文摘录必须逐字校验，编造即丢弃
3. **复盘不编造因果**：数据不足写「不足以判断」
4. **玄学类目合规红线**：绝对化承诺、封建迷信敏感词、恐吓式表述、医疗断言（阿证专职核查）
5. **API Key 不随 exe 发布、不入 git**

**工作流四阶段约定**（老板拍板，记死）：
1. 产品经理先出用户需求报告（写人话、可执行规格）
2. 前端/后端专家按报告实现
3. 审计专家 + 产品经理联合审查（列必修项）
4. 修完必修 → 打包 → 部署桌面
> 禁止绕过流程；即使"小改动"也要走完整四阶段（可先问老板是否简化为"产品经理一句话方向 + 开工"）。

**配色约定**：股票/数据类涨红跌绿（中国习惯），货币默认 ¥。

---

## 八、打包与部署流程（已验证）

```bash
# 1. 语法检查
python -m py_compile scripts/*.py

# 2. 打包（关键：PYTHONPATH= 清空 WorkBuddy 注入的 safe-delete shim，
#    否则增量 build 时 os.remove 旧 base_library.zip 会被拦截报 SAFE_DELETE_FAIL_CLOSED；
#    --clean 避免旧 build 缓存残留）
PYTHONPATH= python -m PyInstaller copywriters_chat.spec --noconfirm --distpath dist --workpath build --clean

# 3. 覆盖桌面（先确认无运行进程，os.replace 比直接写更稳）
#    dst: C:\Users\linsh\Desktop\靓仔文案工作台.exe
```

**单实例机制**：desktop_app.py 用 CreateMutexW 命名互斥体（use_last_error=True 捕获 ERROR_ALREADY_EXISTS=183），第二实例探活旧后端后激活旧窗口并退出。

**坑**：
- exe 覆盖被拒 → OneDrive 或运行中进程锁定，先杀进程再 os.replace
- PyInstaller 沙箱拦截 → `PYTHONPATH=` + 非沙箱模式
- f2 子模块漏打包 → spec 里显式 collect_submodules('f2.apps.douyin/weibo/tiktok')

---

## 九、已知坑与修复记录（关键经验）

### 1. 洗稿报「模型服务连接失败 / 400 input length too long」（已修复，2026-08-25）
- **根因**：旧版 Agent.say() 无输入长度兜底，专家知识档案原样塞进 system prompt，阶段2 全员分析 9 专家并发时输入超限 → DeepSeek 400。旧代码又把错误归一化成"模型服务连接失败"掩盖真相。
- **证据链**：同一篇稿子 12:04 成功、14:39 重做失败；三通道 API 实测正常；失败会话卡在阶段2。
- **修复**：`_MAX_MSG_CHARS=30000` 双层兜底 + `_KNOWLEDGE_CAP_CHARS=12000` + `_friendly_flow_error` 透出真实错误 + 硬超时。
- **教训**：**改完代码必须重新打包 exe 再交付**——代码修复了但没打包，老板用的还是旧 exe。

### 2. 瞬时 401「AI 服务鉴权失败」（2026-08-25）
- 15:15 老板点继续时 deeprouter/deepseek 网关瞬时鉴权波动，几分钟后自愈。
- 修复了两处瑕疵：错误信息"阿骨 阿骨"重复（_raise_if_agent_error 外层 [{name}] 与 reply 自带 [name] 拼重）；"未知错误"糊弄（say() 加 BaseException 兜底 + err=None 明确诊断）。

### 3. 微博抓取
- 游客态被风控 → 需登录态 cookie（SUB 字段），手动配置优先于浏览器自动读取（browser_cookie3 有管理员权限坑）
- **时长单位 bug**：media_info.duration 是秒，存 duration_ms 要 ×1000，否则全部被 <60 秒过滤

### 4. ASR 只扒出一段
- 音乐源（mp3 无人声）转写空 → 改多源重试：按路径 key 去重 + 限 5 源（音乐 2 + 视频 3），转写空自动换 mp4

### 5. WebView2 白屏
- onefile 下 storage_path 必须用 webview.start(storage_path=...) 传参（pywebview 属性赋值无效）

### 6. 音频路线 A/B 标错
- 师傅-求测者咨询视频被全标 A → 加奇偶交替 A/B 兜底 + 历史数据实时修复

---

## 十、装备现状（2026-08-22 盘点）

- **WorkBuddy 技能**：抖音系 5 / 小红书系 7 / 公众号系 8 / multi-wordcheck / jiangai 降AI 等已装齐；缺口：快手·B站爬取、AI 生图封面（建议接可灵 AI 连接器）
- **连接器**：仅 agent-mail、ima-mcp 已连接；推荐连接：可灵 AI（生图）、企业微信（监控提醒）、腾讯文档（报告同步）
- **自动化**：3 个（抖音对标账号日报 09:00 / 爆款晨报·公众号 08:30 / 抖音飙升榜·选题雷达 10:00）；对标账号日报待老板提供真实抖音号
- **专家**：已激活「爆款炼金师」，方法论已蒸馏为第 4 份方法论 `knowledge/skills_methods/viral-topic-methods.md`（四基因+8维打分+传播锚点）

---

## 十一、遗留事项与下一步

1. **数据复盘闭环优化**（未完成，2026-08-24 暂存）——老板原话要求存长期记忆，下次提"继续上次的数据复盘优化"再继续：
   - 已做：blacklist 加 remedy_text/history/verdict；_review_focused_prompt 锁 focus_sentence；/api/works/<wid>/review 路由；_parse_focused_remedy；复读机去重
   - 未做：discussion 路由强制 focus_sentence_id 入参；新手段吸收 + 无效手段从 lessons 删除；报告前端展示 prev_remedy 对比；负责人自动评估更换（winrate 连续低→换人）
   - **继续前先确认老板对 8 条验收标准的优先级**
2. **对标监控二期**：高赞评论 + 口播逐字稿预载
3. **抖音账号实时视频监控模块**：曾规划，独立于对标监控区块
4. **git 状态**：main 分支已入库（8153fe7 为最新），无远程仓库；根目录有大量 build_*/dist_* 残留目录和调试脚本（_fix_*.py、_patch_*.py、zFIX_*.py 等）可清理
5. **API Key 注意**：config.json 含真实密钥（deeprouter + deepseek），交接后建议轮换

---

## 十二、交接包清单

| 内容 | 说明 |
|---|---|
| 源码（scripts/web/monitor/desktop_app.py/spec） | 完整可运行，测试与调试脚本已剔除 |
| knowledge/skills_methods/ | 4 份方法论档案 |
| knowledge_digests/ | 8 位专家内化知识档案 + lessons |
| knowledge/agent_*/ | 原始知识库（3 个目录） |
| config.json | **已脱敏**（api_key 替换为占位符），真实密钥请从运行数据目录 %APPDATA%\靓仔文案工作台\config.json 获取 |
| 软件功能全景梳理.md / SESSION-STATE.md / README.md / CODEBUDDY.md / MEMORY.md | 既有文档 |
| 项目交接文档.md | 本文档 |
| 桌面 exe | 不随包（已在 C:\Users\linsh\Desktop\靓仔文案工作台.exe，107MB，2026-08-25 版） |
